# Wiki Log

Append-only chronological record. One entry per ingest / query worth filing / lint pass.

Format: `## [YYYY-MM-DD] {ingest|query|lint} | {short title}` followed by 1–3 lines of what happened and which pages were touched.

Tip: `grep "^## \[" log.md | tail -10` shows recent activity.

---

## [2026-05-19] ingest | Al-Omais Market onboarding + rule harvest

First live onboarding via Claude Code API skill (no browser). Merchant: Raihanah Alomais / Al-Omais Market,
Sole Proprietor, San Francisco CA. App 762713 filled and validated. New rules discovered and codified:
- API type `"Individual"` (not `"SoleProprietorship"`) for sole prop
- Principal address country = 199 (not 229 — 229 shows Afghanistan in UI)
- Sole Prop EIN = SSN
- Any govt ID (passport, immigrant card) → DL fields, type 18 upload
- Blank check accepted as voided check
- Multi-page PDF must be split per document before upload
- `processing.banks[]` (not top-level `bankAccounts`); `"dba"` object (not `"dbas"` array)
- Founded date fallback = 1st of current month (not passport issue date)
Pages updated: `concepts/application-onboarding.md`, `wiki/log.md`.

## [2026-05-19] lint | test-app pool update + no-fabrication rule

Sanitised apps 762713 and 762710 (were "MT Electronics Inc" with real principal data — invented name incident).
Added 5 new pool entries (764333, 764195, 764192, 762713, 762710). Added ⛔ no-fabrication rule block to
`concepts/application-onboarding.md`. Updated `kitpos-operator/src/system-prompt.ts` (Rule 3 expanded,
Rule 5 "never POST new app" added as top-level operating rule).

## [2026-05-07] init | wiki scaffolded
Created `wiki/` (index, log, overview, entities/, concepts/, sources/) and root `CLAUDE.md` schema. Raw sources live in `Context/`. No pages ingested yet.

## [2026-05-07] ingest | WhatsApp — Gahl Oren (2026-01-16 → 2026-05-05)
Ingested `Context/WhatsApp Chat - Gahl Oren/_chat.txt` (2494 lines). Created source page `sources/whatsapp-gahl-oren.md`, entity pages for Gahl/Ran/Bakil/KIT POS org, and concept pages for application-onboarding, file-build-flow, dda-update-flow, pricing-change-flow, sunmi-provisioning, kit-apps-by-device, delivery-vendors. Audio attachments (92 .opus files) skipped — no transcripts. Index updated.

## [2026-05-07] ingest | API docs (kit-api-reference.html + swagger.json ×2)
Ingested `Context/kit-api-reference.html` (KIT POS / Maverick API, 33 KB) and `Context/swagger.json` + `Context/swagger (1).json` (identical files, Uber Public API for delivery + Eats, OpenAPI 3.0.1, 75 paths / 30 tags). Created `sources/api-docs.md`, `concepts/kit-api.md` (points to memory `reference_kit_api_endpoints.md` for the canonical endpoint table), `concepts/uber-public-api.md`. Index updated.

## [2026-05-07] ingest | Context/ scaffolding (MD dumps, JS drafts, N8N, data exports)
Read `MAVERICK_CHAT_CONTEXT.md`, `NEW_CHAT_CONTEXT.md`, `MONOREPO_STATUS.md`, `README.md` — confirmed stale 2026-04-24 snapshots (2-agent state). Skimmed JS scaffolding files, N8N exports, data JSONs. Created single source page `sources/context-scaffolding.md` cataloguing them with pointers to current truth (live agent code, root docs, memory). VIBEPROXY-CLAUDE-CODE-SETUP.md marked off-topic, not ingested.

## [2026-05-07] tooling | memory-compiler + Obsidian wired up
Installed `uv` (0.11.11) + cloned `coleam00/claude-memory-compiler` to `.claude-memory-compiler/` (gitignored). Hooks (SessionStart/PreCompact/SessionEnd) registered in `.claude/settings.json` with `cd` prefix. Smoke-tested session-start.py — empty-index output OK. Obsidian vault config: `attachmentFolderPath=Context/clipped`, relative markdown links, `alwaysUpdateLinks`. Updated CLAUDE.md with UI/tooling section. `.gitignore` now excludes `.claude-memory-compiler/` and personal Obsidian workspace state.

## [2026-05-08] tooling | memory compiler fixed
Diagnosed 2026-05-07 22:40 SessionEnd `FLUSH_ERROR`: bundled `claude_agent_sdk` CLI exited with code 1, stderr suppressed via `DEVNULL`. Three defensive fixes: (1) inject explicit PATH (`/opt/homebrew/bin`, `/usr/local/bin`, `~/.local/bin`, `~/.claude/local`) into spawn env in both `session-end.py` and `pre-compact.py`; (2) capture stderr to `scripts/flush.stderr.log` with per-invocation timestamp markers, replacing `DEVNULL`; (3) override `ClaudeAgentOptions.cli_path` in `flush.py` with the system-installed `claude` binary (auth'd via subscription) when present, falling back to bundled. End-to-end test passed: hook → spawn → flush.py → system claude → daily log entry. `.venv` was healthy — task brief's bug 1 didn't apply on this machine. Daily logs now auto-generate on SessionEnd.

## [2026-05-10] build | kitpos-operator Path C — Telegram-driven onboarding agent
End-to-end document-driven merchant onboarding now ships through `@kit_operator_bot` on Render. Six checkpoints in one session: storage bucket + pg_cron 7-day purge (`kit-onboarding-uploads`), Telegram media handlers (photos + PDFs land in Supabase, marker entries appear in conversation history, `/reset` clears both), vision parser via Gemma-4 26B free with retry+fallback chain across 5 free vision models and a persistent `kit_document_parses` cache, sharp-based crops with auto-forward to Telegram for MICR/holder/DL verification, 7 boarding-app CRUD tools mirroring the Python `MerchantOnboardingService` (typed `OnboardingProfile`, all wiki defaults baked into `buildPutBody`, ZIP→5-digit, internal state/country IDs), and a 6-phase onboarding playbook in the system prompt. 12 tools live in three groups (Lookup / Documents / Boarding CRUD). Status messages localised RU/EN per detected input language. Hard rules: only DL + voided check attached, never click ACTIVATE, multi-member LLC detection via Form 1065, voided-check-holder must match EIN legal name. Open architectural question filed in `concepts/kit-operator-agent.md` § "Known issues": current split of text-LLM (gpt-oss-120b) + vision-LLM (gemma-4-26b) burns free-pool quota 2× faster than necessary; a single multimodal-with-tools model would simplify ~600 lines of plumbing. Today's session burnt the daily free vision quota; e2e onboarding test deferred until quota resets. New page `concepts/kit-operator-agent.md` documents the full architecture and tool surface.

## [2026-05-10] sanitise | Test app 758354 ("Minna Mart") restored to placeholder
Operator reported staff almost processed test app 758354 because its data ("Minna mart" / "Mohamed Manea" / Bank of America routing 121042882) looked real. Overwrote all fields with the standard placeholder (Test Company LLC, EIN 111111111, 123 Test Street, Test User CEO, 000000000/0000000000 bank) and added `serviceDescription: "TEST APPLICATION — DO NOT PROCESS"`. Unlinked attached document 29351960 (was a real DL/check tied to the prior occupant). Codified the new rule in `concepts/application-onboarding.md` § "Test-app pool" — sanitised slots must use obviously-fake values + the DO NOT PROCESS marker.

## [2026-05-10] session | First Kit-driven onboarding (Smoker Friendly Livermore) + API rules captured
End-to-end onboarded SMOKER FRIENDLY LIVERMORE LLC (MID pending, app_id 756692) using API directly from this Claude Code session — Path B from the agreed three-path plan (kitpos-operator can't read documents yet). Three docs ingested: IRS EIN CP575B (April 2026), CA Seller's Permit 245409472-00001 (start 2021-11-01), and a Chase voided check. Bank verified by user against MICR crops: routing 322271627 / account 2909287953. Reused existing test app 756692 (overwriting "Test Company LLC") instead of creating new — token can't delete, only processor purges. Mapped EIN responsible party "ABDULAZIZ M ALAJJI MBR" → principal CEO/100%, dropped middle name per `AGENT_CONTEXT.md` rule. MCC 5993 (Cigar Stores) = id 791 found via paginating `/boarding-application/mcc`. Cleared lingering test data with empty strings (`null` is ignored by KIT API); ZIP+4 truncated to 5 digits. Attached only the voided check (type 6); did not attach EIN or Seller Permit per the "DL + Voided Check only" rule. Validate returns 6 expected gaps (business phone/email, principal phone/email/DOB/SSN) — DL not provided yet. Captured every API ergonomic into `concepts/application-onboarding.md` § "API-driven onboarding rules" so the future Kit operator playbook can follow them without rediscovering. Open issue: SMOKER FRIENDLY LIVERMORE has Form 1065 due → multi-member LLC → 100% ownership likely wrong, awaiting operator confirmation.

## [2026-05-08] session | Stage-2 click-through verified + re-fire technique
End-to-end clicked through BroadPOS Sierra Stage 2 on Alshuja Market 1240490019 (A80, MID 201100308288) via `claude-in-chrome` in Chrome. PREVIOUS from Stage 3 → walked RECEIPT (10) → TSYS (13) → MISC (1) — all 24 workflow field IDs from `paxstore_v2/field_ids.py` resolved via `getElementById` in the live React form. NEXT triggered the green `App parameter saved successfully` toast and advanced Stage 2 → Stage 3. Discovered that **re-firing `input`/`change` events with the existing value via the native HTMLInputElement value setter persists the form** — no value change needed, no per-field Playwright `fill()`, one JS call per sub-tab. Updated `concepts/file-build-flow.md` with a new "Stage-2 form persistence — re-fire technique" subsection. Side note: a Chrome password-manager extension on `auth.paxstore.us` blocked all MCP clicks until manual login; problem disappears once redirected to `/admin`. Headless Playwright won't see this.

## [2026-05-16] plan | KIT Label Printer concept page for Sunmi V3 Plus
First-task briefing filed at `concepts/kit-label-printer.md` ahead of Claude Code scaffolding. Locked SDK choice: **Sunmi Inner Printer SDK** (Inbuilt Non-detachable Printers family), NOT Dothantech LPAPI that sits in `~/GitHub/kit_mobile/`. LPAPI demo kept as reference for a possible external-printer future, but V3 Plus prints with its own 80 mm built-in head in label mode (gap-sensor, die-cut paper supported per Sunmi spec). Architecture: single Kotlin/Compose APK, three layers (UI / Domain / Hardware bindings), domain layer ignorant of Sunmi SDK so external-printer swap stays cheap. Distribution: Sunmi MDM Private App on the existing Entity `ZE2FVADC6IG5Y` — no public Sunmi Store needed at any stage. First scope is one flow: side-trigger scan → KIT API `lookup by barcode` → render bitmap → print. 7-step milestone roadmap to pilot (~10 working days once device is on hand). Open questions queued for when the unit arrives: exact SDK version per firmware, KeyEvent vs Scanner SDK for the side trigger, label-mode paper-out reliability, KIT API endpoint name for barcode lookup. Index updated.

## [2026-05-16] update | KIT POS ESL product framing locked + reply sent to Howar
Nikita sent the pilot-intent reply to Howar (full draft + change history in `Research/2026-05-15-zhsunyco-reply-draft.md`). Product framing for the ESL feature locked in: **boutique sale/promo tags, NOT full-shelf replacement**. 50–100 color tags per store, manual toggle from the KIT POS Android app when the merchant starts a sale, no real-time refresh required. This strengthens the Android-on-device deployment path — multi-tenant cloud is now strictly a fallback, not a serious alternative. Updated `concepts/zhsunyco-integration-plan.md` with a new "KIT POS use case" section and `concepts/esl.md` to lead with the boutique framing. Awaiting Howar's response on: Android SDK existence, dev-kit price, tag prices, North American reference customers.

## [2026-05-15] ingest | Zhsunyco ESL — eRetail 3.2 manual + 2.4 GHz overview
Howar Chow forwarded the eRetail 3.2 REST integration manual (v1.1, 2024-07-22) and the 2.4 GHz hardware brochure on 2026-05-15 after Gahl looped Nikita in. Filed both into `Context/` (Zhsunyco-eRetail-3.2-Integration-Manual-v1.1.docx, Zhsunyco-2.4GHz-ESL-Solutions-Overview.pdf). Created `entities/zhsunyco.md` (contacts, product line, status), `concepts/esl.md` (vendor-neutral architecture + KIT POS shortlist from existing `Context/ESL_Vendor_Highlights_KIT_POS_1.pdf`), `sources/zhsunyco-eretail-3.2-manual.md` (~25 endpoints catalogued — auth, tag lifecycle, push data + image, queries, refresh, store CRUD, callbacks, products), `sources/zhsunyco-2.4ghz-esl-overview.md` (ETAP05 base station, 13-SKU tag catalog, reference deployment), and `concepts/zhsunyco-integration-plan.md` — recommends a two-track plan: ask for samples + FCC IDs + multi-tenant licensing + webhook auth in parallel with a 2-day engineering REST spike against a Howar-hosted demo, then re-validate against Solum Newton before signing. Open hard gates: FCC compliance for US deployment, Android-SDK-vs-REST contradiction with the internal vendor highlights doc, no documented auth on the `/apheart` and `/callback` callbacks. Index updated.

## [2026-05-15] ingest | Sunmi provisioning ticket — form-field reference
Gahl re-shared the Sunmi service-desk ticket workflow on 2026-04-19 (WhatsApp) with screenshots of every form field for the canonical Provision-Systems ticket. The existing `concepts/sunmi-provisioning.md` covered the URL + Entity ID + three-step flow but not the per-field values. Added a "Form fields (canonical values)" subsection (Email confirmation `gahl@kit-pos.com`, NAR Area `Western U.S.`, Summary `Provision Systems for KIT POS`, Purchased From `Sunmi US Distributors`), an explicit body template (Entity ID + Zone Western US + ROM updated reminder), notes on where the SN list comes from (printed on the box), and what happens after submit. Index hook expanded so the entry signals "runbook with template" instead of just naming the three steps. No new source page — the underlying chat is already covered by `sources/whatsapp-gahl-oren.md`. Frontmatter `updated` bumped to 2026-05-15.

## [2026-05-07] session | PAX Store 2026 UI rediscovery + Q25/A80 onboarding
Onboarded Alshuja Market (MID 201100308288) with Q25 (1920006225) + A80 (1240490019). Discovered PAX Store UI redesigned in 2026: legacy `paxstore_provision_from_pdf.py` breaks past terminal creation. Built v2 Playwright scripts under `agents/maverick-terminal-agent/scripts/{create_terminal,push_template,push_firmware,activate_task,fill_tsys,explore_template}_v2.py`. Mapped new flow: Push Task → +PUSH APP → Push Template (sub-tab); 5 stages; 13 parameter sub-tabs in BroadPOS Sierra; NEXT-on-each-tab persists data. Q25 = "dumb" pinpad (no apps), A80 = smart POS host (template + TSYS + receipt + Internal POS for stand-alone). Updated `concepts/file-build-flow.md` with 2026-UI section, added Q25/A80 to `concepts/kit-apps-by-device.md`, created dev doc `agents/maverick-terminal-agent/docs/PAXSTORE_AUTOMATION_V2.md` with selectors / field IDs. Field IDs preserved from legacy script: `0_tsys_F1_tsys_param_BIN` etc. RECEIPT IDs `*_sys_F2_sys_cap_receiptHeader0..4`. Open issues: POS Internal/External accordion expansion, helper-code duplication across 6 scripts, server.py still subprocess'es legacy script.
